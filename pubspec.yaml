# pubspec.yaml
name: my_uz
description: Scrapowanie planów zajęć UZ

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http:
  html:
  icalendar_parser:
  logger:
  supabase_flutter:
  uuid:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

executables:
  scraper: scraper