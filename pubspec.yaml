name: my_uz
description: Aplikacja do zarządzania planem zajęć UZ

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0 # Do pobierania plików ICS
  logger: ^2.0.0 # Do logowania
  supabase_flutter: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

executables:
  scraper: scraper